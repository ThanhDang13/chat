// import { BaseEntity } from "@api/domain/models/BaseEntity";
// import { Nullable, Paginated, PagingDTO } from "@api/utils/types";

// export interface IBaseRepository<TModel extends BaseEntity<TProps>, TProps, TCondition> {
//   save(model: TModel): Promise<void>;

//   findById(id: string): Promise<Nullable<TModel>>;

//   list(condition: TCondition, paging: PagingDTO): Promise<Paginated<TModel>>;

//   delete(id: string): Promise<void>;
// }
